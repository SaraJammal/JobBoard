
<p>
  <strong>Title:</strong>
  <%= @job.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @job.description %>
</p>
<% if user_signed_in? %>
     <p> <button onclick= "" > Apply for this position </button> </p>
<%end %>
<% if current_user.isAdmin? %>

<%= link_to 'Edit', edit_job_path(@job) %>
  <%end %>
<%= link_to 'Back', jobs_path %>
